apiVersion: v1
kind: Service
metadata:
  name: {{ include "map-provider.fullname" . }}
  labels:
    {{- include "map-provider.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "map-provider.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
