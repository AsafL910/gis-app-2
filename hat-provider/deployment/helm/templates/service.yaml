apiVersion: v1
kind: Service
metadata:
  name: {{ include "hat-provider.fullname" . }}
  labels:
    {{- include "hat-provider.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "hat-provider.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
