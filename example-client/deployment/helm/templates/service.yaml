apiVersion: v1
kind: Service
metadata:
  name: {{ include "example-client.fullname" . }}
  labels:
    {{- include "example-client.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "example-client.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
