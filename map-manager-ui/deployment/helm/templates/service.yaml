apiVersion: v1
kind: Service
metadata:
  name: {{ include "map-manager-ui.fullname" . }}
  labels:
    {{- include "map-manager-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "map-manager-ui.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
