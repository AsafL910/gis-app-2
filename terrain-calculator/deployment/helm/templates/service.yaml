apiVersion: v1
kind: Service
metadata:
  name: {{ include "terrain-calculator.fullname" . }}
  labels:
    {{- include "terrain-calculator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "terrain-calculator.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
